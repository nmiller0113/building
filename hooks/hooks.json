{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "s=\"${CLAUDE_PLUGIN_ROOT:-}/hooks/session-start.py\"; [ -f \"$s\" ] || exit 0; for py in python3 python; do command -v \"$py\" >/dev/null 2>&1 && exec \"$py\" \"$s\"; done; exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "s=\"${CLAUDE_PLUGIN_ROOT:-}/hooks/workorder-gate.py\"; [ -f \"$s\" ] || exit 0; for py in python3 python; do command -v \"$py\" >/dev/null 2>&1 && exec \"$py\" \"$s\"; done; exit 0"
          }
        ]
      }
    ]
  }
}
